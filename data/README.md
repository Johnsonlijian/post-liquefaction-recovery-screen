# Data Directory

This directory intentionally contains only `parquet_coord_bridge.csv`, a small derived coordinate bridge table.

Large or third-party inputs are not redistributed in this repository. To rerun the full pipeline, download or query the sources listed in `DATASETS_AND_LINKS.csv` and place them here using the paths described in `REPRODUCIBLE_RUNBOOK.md`.
